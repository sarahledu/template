import * as React from "react"
import { Link } from "gatsby"

import Layout from "../components/Layout"

const IndexPage = () => (
  <Layout>
    <h1 className="text-4xl font-bold text-palette2">Roule ma poule</h1>
    <div className="text-lg ">Bisous</div>
  </Layout>
)

export default IndexPage
